var React = require("react");

var Invitation = React.createClass({
    
    render: function () {
        
        return (
            <h4>Invitation</h4>    
        );
    }
});

module.exports = Invitation;